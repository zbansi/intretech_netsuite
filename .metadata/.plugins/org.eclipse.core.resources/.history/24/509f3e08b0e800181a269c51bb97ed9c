package com.bansi.oauth;

import com.github.scribejava.*;

public class OauthSignatureFactory{
	OAuthService service = new ServiceBuilder()
	          .apiKey(YOUR_API_KEY)
	          .apiSecret(YOUR_API_SECRET)
	          .build(LinkedInApi20.instance());
}